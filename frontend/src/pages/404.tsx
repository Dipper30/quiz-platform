import './404.less'

type NotFoundProps = {
  
}

const NotFound: React.FC<NotFoundProps> = (props) => {
  return (
    <div className="not-found">
      404 NotFound
    </div>
  )
}

export default NotFound