import './Layout.less'

type HeaderProps = {
  
}

const Header: React.FC<HeaderProps> = (props) => {
  return (
    <div className='admin-header-container'>
      OGD Quiz CRM
    </div>
  )
}

export default Header